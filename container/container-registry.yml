
# 
# K8s nginx compose
# 

version: '1.0'
name: container/container-registry.yml

# 
# TYPES
# 

types:

  ContainerRegistry:
    name: ContainerRegistry
    version: 1
    property:
      name: string
      # url: string
