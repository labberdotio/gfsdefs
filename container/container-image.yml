
# 
# K8s nginx compose
# 

version: '1.0'
name: container/container-image.yml

# 
# TYPES
# 

types:

  ContainerImage:
    name: ContainerImage
    version: 1
    property:
      name: string
      # imageName: string
      # imageVersion: string
      # imageRegistry: "#ContainerRegistry"
      version: string
      registry: "#ContainerImageRegistry"
