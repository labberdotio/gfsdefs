
# 
# template.yml
# 

version: '1.0'
name: gfs/template.yml
types:

  Template:
    name: Template
    version: 1
    property:
      name: string
      template: string
      format: string
      mime: string
    required:
      - name
      - template
      - format
