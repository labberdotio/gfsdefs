
# 
# view.yml
# 

version: '1.0'
name: gfs/view.yml
types:

  View:
    name: View
    version: 1
    property:
      name: string
      mime: string
      query: "#Query"
      template: "#Template"
    required:
      - name
      - query
      - template
