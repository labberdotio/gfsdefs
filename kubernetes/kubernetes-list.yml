
# 
# K8s nginx compose
# 

version: '1.0'
name: kubernetes/kubernetes-list.yml

# 
# TYPES
# 

types:

  KubernetesList:
    name: KubernetesList
    version: 1
    property:
      name: string
      deploymentItems:
        - "#KubernetesDeployment"
      serviceItems:
        - "#KubernetesService"
      ingressItems:
        - "#KubernetesIngress"
