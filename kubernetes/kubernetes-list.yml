
# 
# K8s nginx compose
# 

version: '1.0'
name: kubernetes/kubernetes-list.yml

# 
# TYPES
# 

types:

  KubernetesList:
    name: KubernetesList
    version: 1
    property:
      name: string
      items:
        - "#KubernetesListItem"

  KubernetesListItem:
    name: KubernetesListItem
    version: 1
    property:
      name: string
