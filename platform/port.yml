
# 
# K8s nginx compose
# 

version: '1.0'
name: platform/port.yml

# 
# TYPES
# 

types:

  Port:
    name: Port
    version: 1
    property:
      name: string
      port: string
    required:
      - name
      - port
