
# 
# volume-snapshotter.yml
# 

version: '1.0'
name: platform/volume-snapshotter.yml
types:

  VolumeSnapshotter:
    name: VolumeSnapshotter
    version: 1
    property:
      name: string
      description: string
      volumes:
        - "#Volume"
      snapshots:
        - "#VolumeSnapshot"
      service: "#Service"
    required: []
