
# 
# tftp-service.yml
# 

version: '1.0'
name: ftp/tftp-service.yml
types:

  TFTPService:
    name: 'TFTPService'
    version: 1
    properties:
      name: string
      port: string
      configuredMachine: '#Machine'
    required:
      - name
      - port
