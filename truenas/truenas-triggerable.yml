
# 
# truenas-triggerable.yml
# 

version: '1.0'
name: truenas/truenas-triggerable.yml
types:

  TrueNASTrigger:
    version: 1
    extends: "#Trigger"
    property:
      # chain:
      #   - string
      chain: string
      # 
      # Ultimately I want this
      # 
      # template: "#template"
      template: string
      # 
      # Downsteam dependency, should fix
      # 
      service: "#Service"
    required: []
