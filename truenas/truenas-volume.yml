
# 
# truenas-volume.yml
# 

---
version: '1.0'
types:

  TrueNASVolume:
    version: 1
    extends: "#Volume"
    implements:
      - '#Pulsable'
      - '#Discoverable'
      - '#Triggerable'
    property:
      name: string
      description: string
      media: string
      size: string
      service: "#Service"
    required: []
